[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node dist/scripts/seed-interests.js && node dist/scripts/seed-sources.js && node dist/server.js"

# Cron: configure in Railway dashboard
# Schedule: 0 0 * * *
# Command:  node dist/scripts/run-pipeline.js
